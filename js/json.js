// Pasar Array de productos a JSON
/* 
const localSave = (key,value) => {localStorage.setItem(key,value)};

for (const producto of Products){
    localSave(producto, JSON.stringify(producto));
}

const userSelection = JSON.parse(sessionStorage.getItem())


console.log(userSelection) */